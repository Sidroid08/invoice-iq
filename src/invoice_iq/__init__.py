"""invoice-iq — Invoice Intelligence Platform.

A local-first, MLOps-complete document-AI system for invoices:
ingestion + extraction, a trained PyTorch document classifier, a RAG pipeline,
an agentic LangGraph workflow, and FastAPI serving — with GCP Vertex AI swap-ins.
"""

__version__ = "0.1.0"
