# Pinned dependency mirror of pyproject.toml for pip users.
# Source of truth is pyproject.toml; keep these in sync.
#
# --- Runtime (added per phase) ---
# Phase 1: schemas + typed settings
pydantic==2.13.4
pydantic-settings==2.14.1
email-validator==2.3.0
# Phase 2: ingestion + extraction (local)
pdfplumber==0.11.9
reportlab==4.5.1
# Phase 3: PyTorch classifier. Install the CPU build first to avoid the large
# CUDA wheel: pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.12.0
numpy==2.4.6
# Phase 4: RAG (embeddings + vector store). First use downloads the pinned
# embedding model all-MiniLM-L6-v2 (~80 MB) to ~/.cache/huggingface.
sentence-transformers==5.5.1
chromadb==1.5.9
# Phase 5: agentic orchestration
langgraph==1.2.4
langchain-core==1.4.0
# Phase 2 optional extras (NOT installed by default):
#   OCR fallback (also needs Tesseract binary):  pip install -e ".[ocr]"
#   LLM extraction path (needs ANTHROPIC_API_KEY): pip install -e ".[llm]"
#
# Dev / tooling (install with: pip install -e ".[dev]"  OR  pip install -r requirements.txt)
ruff==0.8.4
mypy==1.13.0
pytest==8.3.4
pytest-cov==6.0.0
